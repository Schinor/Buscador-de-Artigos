# Buscador-de-Artigos
This scientific article search engine, written in Python, uses an LLM (Logical Learning Management) to orchestrate the entire workflow. It searches scientific articles on the requested topic via API, then analyzes the most important data and returns only the most relevant articles.
